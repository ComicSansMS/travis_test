
language: cpp
sudo: false

compiler:
  - gcc

install:
  - env

before_script:
  - cd ${TRAVIS_BUILD_DIR}
  - git clone https://github.com/ComicSansMS/libstratcom.git .
  - cd build
  - cmake -DBUILD_EXAMPLE_APPLICATIONS=ON -DCMAKE_INSTALL_PREFIX=${TRAVIS_BUILD_DIR}/INSTALL ..
  - make

script:
  - cd ${TRAVIS_BUILD_DIR}/build
  - cmake -DBUILD_EXAMPLE_APPLICATIONS=ON -DCMAKE_INSTALL_PREFIX=${TRAVIS_BUILD_DIR}/INSTALL ..
  - make

after_script:
  - cd ${TRAVIS_BUILD_DIR}/build
  - make doxygen-doc
