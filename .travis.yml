
language: cpp
sudo: false

compiler:
  - gcc
addons: &gcc48
  apt:
    packages: gcc-4.8
    sources: *sources

install:
  - gcc --version
  - gcc48 --version
  - g++ --version
  - g++48 --version
  - DEPS_DIR="${TRAVIS_BUILD_DIR}/deps"
  - mkdir ${DEPS_DIR} && cd ${DEPS_DIR}
  - wget http://cmake.org/files/v3.3/cmake-3.3.2.tar.gz
  - tar -xvf cmake-3.3.2.tar.gz > /dev/null
  - mkdir cmake-build && mkdir cmake-install
  - cd cmake-build
  - cmake -DCMAKE_INSTALL_PREFIX=${DEPS_DIR}/cmake-install -DCMAKE_CXX_COMPILE=g++48 -DCMAKE_C_COMPILER=gcc48 ../cmake-3.3.2
  - ls -lha ${DEPS_DIR}/cmake-install
  
  - which cmake

before_script:
  - cd ${TRAVIS_BUILD_DIR}
  - git clone https://github.com/ComicSansMS/GhulbusBase.git gbBase
  - cd gbBase
  - cd build
  - cmake ..

script:
  - make
  - make test
